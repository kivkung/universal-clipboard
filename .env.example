# Optional profile override (set in your shell; this file is not auto-loaded).
# UC_DATA_DIR=/path/to/universal-clipboard-profile
# Or pass: uc --data-dir /path/to/profile setup
# Android APK integration uses docs/PROTOCOL.md; Termux overrides were removed.
